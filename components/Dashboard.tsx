import React from 'react';

// This component is currently unused and deprecated.
// Functionality has been moved to ReportView.
// Recharts dependency has been removed.

const Dashboard: React.FC = () => {
  return (
    <div className="p-8 text-center text-gray-500">
      Dashboard is disabled.
    </div>
  );
};

export default Dashboard;